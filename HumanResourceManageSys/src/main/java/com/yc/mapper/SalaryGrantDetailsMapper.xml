<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yc.mapper.SalaryGrantDetailsMapper">
	<insert id="insertSalaryGrantDetails" parameterType="SalaryGrantDetails">
		insert into salary_grant_details(salary_grant_id,human_id,human_name,bouns_sum,sale_sum,deduct_sum,salary_standard_sum,
		salary_paid_sum) values(#{salary_grant_id},#{human_id},#{human_name},#{bouns_sum},#{sale_sum},#{deduct_sum},
		#{salary_standard_sum},#{salary_paid_sum})
	</insert>
</mapper>